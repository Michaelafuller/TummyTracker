// Drizzle schema — the single source of truth for the data model (CLAUDE.md §6).
// The `logEntry` table is introduced in Phase 1a; this file intentionally starts
// empty so Phase 0 establishes the migration pipeline without any tables yet.
export {};
