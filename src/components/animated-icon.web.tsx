// Web build of the splash overlay never renders one — expo-splash-screen's
// native handoff is a native-platform concept — so there is nothing here to
// recolor for HANDOFF.md Cycle D. (AnimatedIcon, the dead template gradient
// logo this file used to also export, was removed in that same cycle.)
export function AnimatedSplashOverlay() {
  return null;
}
